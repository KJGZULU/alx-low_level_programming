#include "main.h"
/**
 * _isdigit - checking if the character is a digit
 * @x: the checked number
 * Return: 1 for a digit and 0 for any
 */
int _isdigit(int x)
{
if (x >= 48 && x <= 57)
{return (1);
}
return (0);
}
