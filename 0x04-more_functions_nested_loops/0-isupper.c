#include "main.h"
/**
 * _isupper - a function that checks for uppercase character.
 * @x: The checked character
 * Return: 1 for upper and 0 for any
 */
int _isupper(int x)
{
if (x >= 65 && x <=90)
{return (1);
}
return (0);
}
